<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shambu.autoattendance">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UI.AttendanceHistoryTimelineActivity"
            android:theme="@style/AppThemeNAB"></activity>
        <activity
            android:name=".UI.EditSubjectActivity"
            android:theme="@style/AppThemeNAB" />
        <activity
            android:name=".UI.NewSubject"
            android:theme="@style/AppThemeNAB" />

        <activity
            android:name=".UI.SelectClassLocation"
            android:theme="@style/AppThemeNAB" />

        <activity android:name=".UI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".PathsenseGeofenceReceiver" />

        <receiver android:name=".ClassEndingAlarmManagerReceiver"/>

        <service android:name=".DatabaseUpdateService" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/googleApiKey" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <meta-data
            android:name="com.pathsense.android.sdk.CLIENT_ID"
            android:value="@string/pathsenseClientId" />

        <meta-data
            android:name="com.pathsense.android.sdk.API_KEY"
            android:value="@string/pathsenseApiKey" />
    </application>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>